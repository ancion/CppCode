#include <stdio.h>


// inode 是文件的索引节点
// inode 是唯一的，每一个文件的 inode 都是不相同的
// inode 中包含了哪些信息(调用 stat 的时候就是返回了部分信息)
//   > 文件类型
//   > 文件的权限，rwx
//   > uid, gid 
//   > 文件的大小
//   > 时间信息(创建，访问，修改等) 时间

int main(void) {

}
